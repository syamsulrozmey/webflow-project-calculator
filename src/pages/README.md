This directory intentionally stays empty until we introduce legacy `pages/` routes or API endpoints. Prefer the App Router inside `src/app`, but keep this folder available for Next.js features that still rely on `pages`.

